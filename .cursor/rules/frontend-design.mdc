---
description: Frontend design skill — enforces restrained composition, strong hierarchy, design tokens, and tasteful motion. Activates when working on LiveView pages, components, templates, CSS, or JS assets.
globs: ["**/live/**", "**/components/**", "**/css/**", "**/js/**", "assets/**", "**/*.heex"]
alwaysApply: false
---

# Frontend Design — Cursor Auto-Trigger Rule

This rule auto-loads when Cursor opens files matching the globs above.
The full skill content lives in `.agents/skills/frontend-design/SKILL.md` —
read and follow it. That file is the single source of truth for all agents
(Cursor, Claude Code, Codex).

Also read:

- `docs/frontend.md` — component architecture, LiveView patterns.
- `docs/design-system.md` — token foundations, two-tier colors.
- `docs/workflow.md` — design-to-code process.
- `docs/figma-mcp.md` — Figma MCP tool matrix.
- `docs/agentic-design.md` — the loop and anti-stale matrix.
